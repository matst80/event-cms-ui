{
  "name": "vite-template-solid",
  "version": "0.0.0",
  "description": "",
  "scripts": {
    "start": "esbuild --target=es2020 --servedir=www --tree-shaking=true --sourcemap --minify --bundle --outdir=www src/index.tsx"
  },
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.15.5",
    "esbuild-plugin-solid": "^0.4.2",
    "serve": "^14.0.1"
  },
  "dependencies": {
    "jsx-real-dom": "file:./jsx-real-dom"
  }
}